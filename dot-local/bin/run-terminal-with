#!/usr/bin/env nu

def --wrapped main [app: string, ...more_args: string] {
  let app_id_flag = if ($nu.os-info.name == "linux") { ["--app-id" $app]} else { [] }

  kitty --detach ...$app_id_flag --title $app -o tab_bar_min_tabs=5 ...$more_args $app
}
